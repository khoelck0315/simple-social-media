#!/bin/bash

# In addition to the composer install, npm needs to install dependencies too
npm install
npm run build

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc
