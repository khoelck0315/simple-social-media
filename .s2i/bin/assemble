#!/bin/bash
npm --prefix /opt/app-root/src install
npm --prefix /opt/app-root/src install mix
npm --prefix /opt/app-root/src run production

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc
